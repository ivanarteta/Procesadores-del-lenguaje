algoritmo orden3;
    ent a, b, c: entero; 
    sal max, min, med: entero;
{Prec: a = A AND b = B AND c = C}
    si A >= b y a >= c ->
        max := a;
    fsi
{Post: a=A AND b = B AND c= C ...}
falgoritmo